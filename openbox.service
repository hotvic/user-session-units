[Unit]
Description=Openbox
Before=window-manager.target

[Service]
ExecStart=/usr/bin/openbox
ExecReload=/usr/bin/openbox --reconfigure
ExecStop=/usr/bin/openbox --exit
KillMode=process
Restart=always
RestartSec=1

[Install]
WantedBy=window-manager.target
